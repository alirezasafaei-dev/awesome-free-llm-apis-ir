# Evidence semantics hardening

This branch restores the distinction between signup failure and post-signup account activation barriers, removes raw provider error payloads from public evidence, and keeps the direct-route matrix open until identical authenticated tests exist for every provider.

The file exists to make the branch diff explicit before the self-removing patch workflow rewrites the permanent source files. It will be removed by the patch workflow before the final commit.
