server {
    listen 80;
    listen [::]:80;
    server_name ir.llm.persiantoolbox.ir;

    root /srv/awesome-free-llm-apis-ir/current;
    index index.html;
    error_page 404 /404.html;

    # Effective when Certbot adds the HTTPS listener to this server block.
    # Browsers ignore this header on the plain HTTP response.
    add_header Strict-Transport-Security "max-age=31536000" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-Frame-Options "DENY" always;
    add_header Referrer-Policy "strict-origin-when-cross-origin" always;
    add_header Permissions-Policy "camera=(), microphone=(), geolocation=()" always;
    add_header X-Robots-Tag "noindex, nofollow" always;
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'sha256-P3a7n+nc/XUcmtb6hjbo81/2DVp5K6tQtbdsQyR5B9c='; style-src 'self' 'sha256-GQQ/EMIT0d/Yat5f7OoduJP6XQT9Aoo7nEXm+Iqbm5E='; img-src 'self' data:; connect-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'; upgrade-insecure-requests" always;

    # Analytics is intentionally recorded only on the canonical site. The mirror
    # explicitly discards event POSTs rather than returning the SPA fallback HTML.
    location = /api/event {
        return 204;
    }

    location = /catalog.json {
        expires 5m;
        try_files $uri =404;
    }

    location = /build-meta.json {
        expires -1;
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }

    location = /404.html {
        internal;
    }

    access_log /var/log/nginx/ir.llm.persiantoolbox.ir.access.log;
    error_log /var/log/nginx/ir.llm.persiantoolbox.ir.error.log warn;
}
